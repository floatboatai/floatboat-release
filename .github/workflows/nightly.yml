name: Nightly Build

# 每天北京时间 00:00 (UTC 16:00) 自动构建 dev 分支
on:
  schedule:
    - cron: '0 16 * * *'
  workflow_dispatch:
    inputs:
      ref:
        description: 'Branch or ref to build (default: dev)'
        required: false
        default: 'dev'
        type: string
      platforms:
        description: 'Platforms to build'
        required: false
        default: 'macos,windows'
        type: choice
        options:
          - 'macos,windows'
          - 'macos'
          - 'windows'

permissions:
  contents: write
  actions: read

jobs:
  prepare:
    name: Prepare Nightly
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.nightly_version }}
      ref: ${{ steps.ref.outputs.ref }}
      platforms: ${{ steps.config.outputs.platforms }}
      prev_tag: ${{ steps.changelog.outputs.prev_tag }}
      changelog_file: ${{ steps.changelog.outputs.changelog_file }}

    steps:
      - name: Determine ref
        id: ref
        run: |
          if [ "${{ github.event_name }}" == "schedule" ]; then
            echo "ref=dev" >> $GITHUB_OUTPUT
          else
            echo "ref=${{ github.event.inputs.ref || 'dev' }}" >> $GITHUB_OUTPUT
          fi

      - name: Checkout source repo
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.SOURCE_REPO }}
          ref: ${{ steps.ref.outputs.ref }}
          token: ${{ secrets.SOURCE_REPO_PAT }}
          fetch-depth: 0

      - name: Determine nightly version
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          DATE=$(date -u +%Y%m%d)
          NIGHTLY="${VERSION}-n.${DATE}"
          echo "nightly_version=$NIGHTLY" >> $GITHUB_OUTPUT
          echo "📦 Nightly version: $NIGHTLY"

      - name: Read config
        id: config
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "platforms=${{ github.event.inputs.platforms || 'macos,windows' }}" >> $GITHUB_OUTPUT
          else
            echo "platforms=macos,windows" >> $GITHUB_OUTPUT
          fi

      - name: Generate internal nightly release note
        id: changelog
        run: |
          set -e
          VERSION="${{ steps.version.outputs.nightly_version }}"
          CURRENT_TAG="nightly-${VERSION}"

          # Nightly builds are built from the source repository. Compare source HEAD
          # with the previous reachable source tag, not release-repo nightly tags.
          PREV_TAG=$(git describe --tags --abbrev=0 2>/dev/null || true)
          CHANGELOG_FILE="nightly-changelog-${PREV_TAG:-initial}-${CURRENT_TAG}.md"
          CHANGELOG_FILE=$(echo "$CHANGELOG_FILE" | tr '/ ' '--')

          echo "prev_tag=${PREV_TAG}" >> $GITHUB_OUTPUT
          echo "changelog_file=${CHANGELOG_FILE}" >> $GITHUB_OUTPUT

          {
            echo "# Nightly ${CURRENT_TAG}"
            echo ""
            if [ -n "$PREV_TAG" ]; then
              echo "**Changes since ${PREV_TAG}:**"
              echo ""
              CHANGES=$(git log "${PREV_TAG}..HEAD" --no-merges --format="- %s" | head -200)
              if [ -n "$CHANGES" ]; then
                echo "$CHANGES"
              else
                echo "_无更新。_"
              fi
            else
              echo "_This is the initial nightly build — no previous source tag found._"
            fi
            echo ""
            echo "---"
            echo "*Generated $(date -u '+%Y-%m-%d %H:%M UTC') · Internal nightly release note for Feishu notification only.*"
          } > "/tmp/${CHANGELOG_FILE}"

          echo "✅ Nightly changelog: ${CHANGELOG_FILE}"
          echo "🔖 Previous source tag: ${PREV_TAG:-none}"
          head -30 "/tmp/${CHANGELOG_FILE}"

      - name: Prepare empty public release body
        run: |
          # Nightly release notes must stay internal-only; keep the public GitHub Release body empty.
          : > /tmp/release_notes.md

      - name: Upload empty public release body
        uses: actions/upload-artifact@v4
        with:
          name: nightly-release-notes
          path: /tmp/release_notes.md
          retention-days: 1

      - name: Upload internal nightly release note
        uses: actions/upload-artifact@v4
        with:
          name: nightly-internal-release-note
          path: /tmp/nightly-changelog-*.md
          retention-days: 7

  build-macos-arm64:
    name: Build macOS (arm64)
    needs: prepare
    runs-on: macos-latest
    if: contains(needs.prepare.outputs.platforms, 'macos')

    steps:
      - name: Checkout source repo
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.SOURCE_REPO }}
          ref: ${{ needs.prepare.outputs.ref }}
          token: ${{ secrets.SOURCE_REPO_PAT }}
          submodules: recursive

      - name: Inject version
        run: |
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            pkg.version = '${{ needs.prepare.outputs.version }}';
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Gemini CLI
        run: pnpm run build:gemini

      - name: Check code signing
        id: signing
        run: |
          if [ -n "$APPLE_CERTIFICATE" ]; then
            echo "available=true" >> $GITHUB_OUTPUT
          else
            echo "available=false" >> $GITHUB_OUTPUT
          fi
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}

      - name: Import Certificate
        if: steps.signing.outputs.available == 'true'
        uses: apple-actions/import-codesign-certs@v2
        with:
          p12-file-base64: ${{ secrets.APPLE_CERTIFICATE }}
          p12-password: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}

      - name: Build arm64
        run: pnpm run forge:make:arm64
        env:
          NODE_OPTIONS: --max-old-space-size=8192
          BUILD_OBFUSCATE: "true"
          APPLE_IDENTITY: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_IDENTITY || '' }}
          APPLE_ID: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_ID || '' }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_APP_SPECIFIC_PASSWORD || '' }}
          APPLE_TEAM_ID: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_TEAM_ID || '' }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nightly-macos-arm64
          path: |
            out/make/**/*.dmg
            out/make/**/*.zip
          retention-days: 7

  build-macos-x64:
    name: Build macOS (x64)
    needs: prepare
    runs-on: macos-latest
    if: contains(needs.prepare.outputs.platforms, 'macos')

    steps:
      - name: Checkout source repo
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.SOURCE_REPO }}
          ref: ${{ needs.prepare.outputs.ref }}
          token: ${{ secrets.SOURCE_REPO_PAT }}
          submodules: recursive

      - name: Inject version
        run: |
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            pkg.version = '${{ needs.prepare.outputs.version }}';
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Gemini CLI
        run: pnpm run build:gemini

      - name: Check code signing
        id: signing
        run: |
          if [ -n "$APPLE_CERTIFICATE" ]; then
            echo "available=true" >> $GITHUB_OUTPUT
          else
            echo "available=false" >> $GITHUB_OUTPUT
          fi
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}

      - name: Import Certificate
        if: steps.signing.outputs.available == 'true'
        uses: apple-actions/import-codesign-certs@v2
        with:
          p12-file-base64: ${{ secrets.APPLE_CERTIFICATE }}
          p12-password: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}

      - name: Build x64
        run: pnpm run forge:make:x64
        env:
          NODE_OPTIONS: --max-old-space-size=8192
          BUILD_OBFUSCATE: "true"
          APPLE_IDENTITY: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_IDENTITY || '' }}
          APPLE_ID: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_ID || '' }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_APP_SPECIFIC_PASSWORD || '' }}
          APPLE_TEAM_ID: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_TEAM_ID || '' }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nightly-macos-x64
          path: |
            out/make/**/*.dmg
            out/make/**/*.zip
          retention-days: 7

  build-windows:
    name: Build Windows (x64)
    needs: prepare
    runs-on: windows-latest
    if: contains(needs.prepare.outputs.platforms, 'windows')

    steps:
      - name: Checkout source repo
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.SOURCE_REPO }}
          ref: ${{ needs.prepare.outputs.ref }}
          token: ${{ secrets.SOURCE_REPO_PAT }}
          submodules: recursive

      - name: Inject version
        shell: bash
        run: |
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            pkg.version = '${{ needs.prepare.outputs.version }}';
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Gemini CLI
        run: pnpm run build:gemini

      - name: Build and package (production)
        run: pnpm run forge:make
        env:
          BUILD_OBFUSCATE: "true"

      - name: Collect production Windows artifact
        shell: bash
        run: |
          mkdir -p dist/windows
          find out/make -name "*.exe" | while read f; do
            dir=$(dirname "$f")
            base=$(basename "$f")
            newname=$(echo "$base" | tr ' ' '-')
            if [ "$base" != "$newname" ]; then
              mv "$f" "$dir/$newname"
              echo "Renamed: $base -> $newname"
            fi
          done
          PROD_EXE=$(find out/make -type f -name "*.exe" | head -1)
          [ -n "$PROD_EXE" ] || { echo "Production EXE not found"; exit 1; }
          cp "$PROD_EXE" "dist/windows/"
          echo "✅ Production Windows artifact: $(basename "$PROD_EXE")"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nightly-windows-x64
          path: |
            dist/windows/*.exe
          retention-days: 7
          if-no-files-found: error

  build-test-launcher-macos-arm64:
    name: Build Test Launcher macOS (arm64)
    needs: prepare
    runs-on: macos-latest
    if: contains(needs.prepare.outputs.platforms, 'macos')

    steps:
      - name: Checkout source repo
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.SOURCE_REPO }}
          ref: ${{ needs.prepare.outputs.ref }}
          token: ${{ secrets.SOURCE_REPO_PAT }}
          submodules: recursive

      - name: Inject version
        run: |
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            pkg.version = '${{ needs.prepare.outputs.version }}';
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Gemini CLI
        run: pnpm run build:gemini

      - name: Check code signing
        id: signing
        run: |
          if [ -n "$APPLE_CERTIFICATE" ]; then
            echo "available=true" >> $GITHUB_OUTPUT
          else
            echo "available=false" >> $GITHUB_OUTPUT
          fi
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}

      - name: Import Certificate
        if: steps.signing.outputs.available == 'true'
        uses: apple-actions/import-codesign-certs@v2
        with:
          p12-file-base64: ${{ secrets.APPLE_CERTIFICATE }}
          p12-password: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}

      - name: Package test launcher (macOS arm64)
        run: pnpm run package:test-launcher:macos
        env:
          NODE_OPTIONS: --max-old-space-size=8192
          APPLE_IDENTITY: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_IDENTITY || '' }}
          APPLE_ID: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_ID || '' }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_APP_SPECIFIC_PASSWORD || '' }}
          APPLE_TEAM_ID: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_TEAM_ID || '' }}

      - name: Collect test launcher artifact
        shell: bash
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          SRC="tools/floatboat-test-launcher/floatboat-test-launcher-macos.zip"
          [ -f "$SRC" ] || { echo "Launcher zip not found: $SRC"; exit 1; }
          mkdir -p dist/launchers
          cp "$SRC" "dist/launchers/floatboat-test-launcher-${VERSION}-macos-arm64.zip"
          echo "✅ Test launcher (macOS arm64): floatboat-test-launcher-${VERSION}-macos-arm64.zip"

      - name: Upload test launcher artifact
        uses: actions/upload-artifact@v4
        with:
          name: nightly-test-launcher-macos-arm64
          path: dist/launchers/*.zip
          retention-days: 7
          if-no-files-found: error

  build-test-launcher-macos-x64:
    name: Build Test Launcher macOS (x64)
    needs: prepare
    runs-on: macos-13
    if: contains(needs.prepare.outputs.platforms, 'macos')

    steps:
      - name: Checkout source repo
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.SOURCE_REPO }}
          ref: ${{ needs.prepare.outputs.ref }}
          token: ${{ secrets.SOURCE_REPO_PAT }}
          submodules: recursive

      - name: Inject version
        run: |
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            pkg.version = '${{ needs.prepare.outputs.version }}';
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Gemini CLI
        run: pnpm run build:gemini

      - name: Check code signing
        id: signing
        run: |
          if [ -n "$APPLE_CERTIFICATE" ]; then
            echo "available=true" >> $GITHUB_OUTPUT
          else
            echo "available=false" >> $GITHUB_OUTPUT
          fi
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}

      - name: Import Certificate
        if: steps.signing.outputs.available == 'true'
        uses: apple-actions/import-codesign-certs@v2
        with:
          p12-file-base64: ${{ secrets.APPLE_CERTIFICATE }}
          p12-password: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}

      - name: Package test launcher (macOS x64)
        run: pnpm run package:test-launcher:macos
        env:
          NODE_OPTIONS: --max-old-space-size=8192
          APPLE_IDENTITY: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_IDENTITY || '' }}
          APPLE_ID: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_ID || '' }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_APP_SPECIFIC_PASSWORD || '' }}
          APPLE_TEAM_ID: ${{ steps.signing.outputs.available == 'true' && secrets.APPLE_TEAM_ID || '' }}

      - name: Collect test launcher artifact
        shell: bash
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          SRC="tools/floatboat-test-launcher/floatboat-test-launcher-macos.zip"
          [ -f "$SRC" ] || { echo "Launcher zip not found: $SRC"; exit 1; }
          mkdir -p dist/launchers
          cp "$SRC" "dist/launchers/floatboat-test-launcher-${VERSION}-macos-x64.zip"
          echo "✅ Test launcher (macOS x64): floatboat-test-launcher-${VERSION}-macos-x64.zip"

      - name: Upload test launcher artifact
        uses: actions/upload-artifact@v4
        with:
          name: nightly-test-launcher-macos-x64
          path: dist/launchers/*.zip
          retention-days: 7
          if-no-files-found: error

  build-test-launcher-windows:
    name: Build Test Launcher Windows (x64)
    needs: prepare
    runs-on: windows-latest
    if: contains(needs.prepare.outputs.platforms, 'windows')

    steps:
      - name: Checkout source repo
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.SOURCE_REPO }}
          ref: ${{ needs.prepare.outputs.ref }}
          token: ${{ secrets.SOURCE_REPO_PAT }}
          submodules: recursive

      - name: Inject version
        shell: bash
        run: |
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            pkg.version = '${{ needs.prepare.outputs.version }}';
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Gemini CLI
        run: pnpm run build:gemini

      - name: Package test launcher (Windows x64)
        run: pnpm run package:test-launcher:win
        env:
          NODE_OPTIONS: --max-old-space-size=8192

      - name: Collect test launcher artifact
        shell: bash
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          SRC="tools/floatboat-test-launcher/floatboat-test-launcher-win.zip"
          [ -f "$SRC" ] || { echo "Launcher zip not found: $SRC"; exit 1; }
          mkdir -p dist/launchers
          cp "$SRC" "dist/launchers/floatboat-test-launcher-${VERSION}-win-x64.zip"
          echo "✅ Test launcher (Windows x64): floatboat-test-launcher-${VERSION}-win-x64.zip"

      - name: Upload test launcher artifact
        uses: actions/upload-artifact@v4
        with:
          name: nightly-test-launcher-windows
          path: dist/launchers/*.zip
          retention-days: 7
          if-no-files-found: error

  upload:
    name: Publish Nightly
    needs: [prepare, build-macos-arm64, build-macos-x64, build-windows, build-test-launcher-macos-arm64, build-test-launcher-macos-x64, build-test-launcher-windows]
    runs-on: ubuntu-latest
    if: |
      always() &&
      (needs.build-macos-arm64.result == 'success' || needs.build-macos-x64.result == 'success' || needs.build-windows.result == 'success')

    steps:
      - name: Checkout source repo (for scripts)
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.SOURCE_REPO }}
          ref: ${{ needs.prepare.outputs.ref }}
          token: ${{ secrets.SOURCE_REPO_PAT }}

      - name: Download release notes
        uses: actions/download-artifact@v4
        with:
          name: nightly-release-notes
          path: /tmp/

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: nightly-*
          path: artifacts/

      - name: Collect artifacts
        run: |
          mkdir -p out/make dist dist/launchers
          # 把 test-launcher zip 单独分流到 dist/launchers，避免被 generate-latest-yml 误当成 app 自动更新包
          find artifacts/ -type d -name 'nightly-test-launcher-*' -exec sh -c 'cp "$1"/*.zip dist/launchers/ 2>/dev/null || true' _ {} \;
          # app 安装包仍进 out/make；排除 launcher 目录
          find artifacts/ -type f \( -name "*.dmg" -o -name "*.zip" -o -name "*.exe" \) -not -path '*/nightly-test-launcher-*/*' -exec cp {} out/make/ \;
          echo "📦 Nightly app artifacts:"
          ls -la out/make/
          echo "🚀 Nightly test launcher artifacts:"
          ls -la dist/launchers/ 2>/dev/null || echo "  (none)"

      - name: Generate macOS Update Metadata
        if: needs.build-macos-arm64.result == 'success' || needs.build-macos-x64.result == 'success'
        run: |
          chmod +x scripts/generate-latest-yml.sh
          ./scripts/generate-latest-yml.sh --platform mac

      - name: Generate Windows Update Metadata
        if: needs.build-windows.result == 'success'
        run: |
          chmod +x scripts/generate-latest-yml.sh
          EXE_FILE=$(find out/make -name "*.exe" | head -1)
          [ -n "$EXE_FILE" ] && ./scripts/generate-latest-yml.sh --platform win "$EXE_FILE"

      - name: Create Nightly Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly-${{ needs.prepare.outputs.version }}
          name: "Nightly ${{ needs.prepare.outputs.version }}"
          body_path: /tmp/release_notes.md
          draft: false
          prerelease: true
          files: |
            out/make/*.dmg
            out/make/*.zip
            out/make/*.exe
            dist/launchers/*.zip
            dist/latest-mac.yml
            dist/latest.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  notify:
    name: Nightly Summary
    needs: [prepare, build-macos-arm64, build-macos-x64, build-windows, upload]
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Download internal nightly release note
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: nightly-internal-release-note
          path: /tmp/nightly-changelog/

      - name: Send Feishu notification
        run: |
          python3 << 'PYEOF'
          import glob
          import json
          import sys
          import textwrap
          import time
          import urllib.request

          version = "${{ needs.prepare.outputs.version }}"
          prev_tag = "${{ needs.prepare.outputs.prev_tag }}"
          macos_arm64 = "${{ needs.build-macos-arm64.result }}" or "skipped"
          macos_x64 = "${{ needs.build-macos-x64.result }}" or "skipped"
          windows = "${{ needs.build-windows.result }}" or "skipped"
          upload = "${{ needs.upload.result }}" or "skipped"

          note_files = glob.glob("/tmp/nightly-changelog/nightly-changelog-*.md")
          if note_files:
              with open(note_files[0], encoding="utf-8") as f:
                  release_note = f.read().strip()
          else:
              release_note = "_无更新。_"

          def icon(s):
              return {"success": "✅", "failure": "❌", "cancelled": "⏹️", "skipped": "⏭️"}.get(s, "❓")

          release_tag = f"nightly-{version}"
          release_base_url = f"https://github.com/floatboatai/floatboat-release/releases/download/{release_tag}"
          installer_names = [
              f"Floatboat-{version}-arm64.dmg",
              f"Floatboat-{version}-x64.dmg",
              f"Floatboat-darwin-arm64-{version}.zip",
              f"Floatboat-darwin-x64-{version}.zip",
              f"Floatboat-Setup-{version}.exe",
          ]
          launcher_names = [
              f"floatboat-test-launcher-{version}-macos-arm64.zip",
              f"floatboat-test-launcher-{version}-macos-x64.zip",
              f"floatboat-test-launcher-{version}-win-x64.zip",
          ]
          installer_links = "\n".join(f"- {name}: {release_base_url}/{name}" for name in installer_names)
          launcher_links = "\n".join(f"- {name}: {release_base_url}/{name}" for name in launcher_names)
          range_hint = f"  (since {prev_tag})" if prev_tag else ""

          header = f"""🌙 Floatboat Nightly v{version}

          构建结果：
            macOS arm64：{icon(macos_arm64)} {macos_arm64}
            macOS x64：  {icon(macos_x64)} {macos_x64}
            Windows x64：{icon(windows)} {windows}
            上传：       {icon(upload)} {upload}

          安装包下载：
          {installer_links}

          测试录制 launcher：
          {launcher_links}

          🔒 Nightly release note 仅内部发送到飞书机器人，不写入公开 GitHub Release 页面。

          ────────────────────────
          Nightly Release Note{range_hint}："""

          full_text = textwrap.dedent(header).strip() + "\n\n" + release_note
          MAX_CHARS = 3200

          def split_text(text, max_chars):
              chunks = []
              remaining = text
              while len(remaining) > max_chars:
                  cut = remaining.rfind("\n", 0, max_chars)
                  if cut < max_chars // 2:
                      cut = max_chars
                  chunks.append(remaining[:cut].rstrip())
                  remaining = remaining[cut:].lstrip()
              if remaining:
                  chunks.append(remaining)
              return chunks or [""]

          chunks = split_text(full_text, MAX_CHARS)
          total = len(chunks)
          webhook_url = "${{ secrets.FEISHU_WEBHOOK_URL }}".strip()

          if not webhook_url or webhook_url == "***":
              print("❌ FEISHU_WEBHOOK_URL is empty or unavailable", file=sys.stderr)
              sys.exit(1)

          print(
              f"📨 Sending internal Feishu nightly note: version={version}, "
              f"prev_tag={prev_tag or 'none'}, chunks={total}, chars={len(full_text)}"
          )

          def parse_response(body):
              try:
                  return json.loads(body)
              except json.JSONDecodeError as exc:
                  raise RuntimeError(f"Feishu returned non-JSON response: {body!r}") from exc

          def assert_feishu_success(data):
              status_code = data.get("StatusCode")
              code = data.get("code")
              if status_code not in (None, 0) or code not in (None, 0):
                  raise RuntimeError(f"Feishu business error: {data}")

          for index, chunk in enumerate(chunks, start=1):
              text = chunk if total == 1 else f"【{index}/{total}】\n{chunk}"
              payload = json.dumps({
                  "msg_type": "text",
                  "content": {"text": text}
              }, ensure_ascii=False)

              req = urllib.request.Request(
                  webhook_url,
                  data=payload.encode("utf-8"),
                  headers={"Content-Type": "application/json; charset=utf-8"},
                  method="POST"
              )
              try:
                  with urllib.request.urlopen(req, timeout=15) as resp:
                      body = resp.read().decode()
                  data = parse_response(body)
                  assert_feishu_success(data)
                  print(
                      f"✅ Feishu delivered nightly chunk [{index}/{total}] "
                      f"chars={len(text)} response={data}"
                  )
              except Exception as e:
                  print(f"❌ Feishu nightly notification failed [{index}/{total}]: {e}", file=sys.stderr)
                  sys.exit(1)
              if index < total:
                  time.sleep(0.5)
          PYEOF
